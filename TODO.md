# TODO

- [ ] Fix `/dashboard` crash caused by missing/incorrect Flask endpoint `inventory.inventory` in `templates/base.html` by adding safe fallback URL generation.
- [ ] Deploy/retest `/dashboard` on Vercel to confirm error is gone.

